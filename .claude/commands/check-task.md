# 커밋 체크

staged 파일을 확인하고, 커밋 메시지를 자동 생성한 후 커밋하고, 이슈 체크박스를 업데이트합니다.

## 실행 조건

사용자가 `git add .` 또는 `git add <파일>`로 파일을 스테이징한 후 이 명령어를 실행합니다.

## 수행 작업

### 1. Staged 파일 확인
```bash
git status --short
git diff --staged --stat
```

staged 파일이 없으면 사용자에게 알려주고 종료합니다.

### 2. 현재 브랜치에서 이슈 번호 추출
```bash
git branch --show-current
```
브랜치명 예시: `feature/#3-user-signup` → 이슈 번호: 3

### 3. 이슈 내용 및 체크박스 확인
```bash
gh issue view {이슈번호} --json body,title -q '.title, .body'
```

이슈의 체크박스 목록을 확인하여 어떤 작업들이 있는지 파악합니다.

### 4. 커밋 메시지 자동 생성

변경된 파일과 이슈 내용을 분석하여 적절한 커밋 메시지를 생성합니다:

**커밋 메시지 형식:**
```
<type>: <제목>

<본문 (선택)>

🤖 Generated with [Claude Code](https://claude.com/claude-code)

Co-Authored-By: Claude Sonnet 4.5 <noreply@anthropic.com>
```

**Type 규칙:**
- `feat`: 새로운 기능 추가
- `fix`: 버그 수정
- `refactor`: 코드 리팩토링
- `docs`: 문서 수정
- `test`: 테스트 코드
- `chore`: 빌드, 설정 변경

### 5. 사용자에게 커밋 메시지 확인

생성된 커밋 메시지를 사용자에게 보여주고 확인을 받습니다.
사용자가 수정을 원하면 수정 후 진행합니다.

### 6. 커밋 실행
```bash
git commit -m "$(cat <<'EOF'
<생성된 커밋 메시지>
EOF
)"
```

### 7. 이슈 체크박스 업데이트

커밋 메시지와 변경 내용을 기반으로 완료된 작업을 파악하고, 해당 체크박스를 업데이트합니다.

**자동 체크박스 매칭 규칙:**
- 파일명/클래스명으로 작업 항목 추론
- 예: `BaseEntity.java` 생성 → "BaseEntity 작성" 체크
- 예: `application.yml` 수정 → "application.yml 작성" 체크

사용자에게 어떤 체크박스를 업데이트할지 확인받고 진행합니다.

```bash
gh issue edit {이슈번호} --body "수정된 본문"
```

### 8. 진행 상황 알림

사용자에게 현재 진행 상황을 알려주세요:
- 완료된 항목 수 / 전체 항목 수
- 남은 작업 목록
- 다음 작업 제안

## 출력 예시

```
📝 Staged 파일:
  M src/main/resources/application.yml
  A src/main/java/com/studyshare/global/common/BaseEntity.java

📋 커밋 메시지 (자동 생성):
feat: BaseEntity 작성

- JPA Auditing을 위한 공통 필드 추가
- createdAt, updatedAt 자동 관리

🤖 Generated with [Claude Code](https://claude.com/claude-code)

Co-Authored-By: Claude Sonnet 4.5 <noreply@anthropic.com>

이 메시지로 커밋하시겠습니까? (y/n)

---

✅ 커밋 완료!
✅ 체크박스 업데이트 완료!

진행 상황: 3/10 완료

완료:
- [x] 도메인별 디렉토리 구조 생성
- [x] application.yml 작성
- [x] BaseEntity 작성

남은 작업:
- [ ] 공통 응답 클래스(ApiResponse) 작성
- [ ] 전역 예외 처리 클래스(GlobalExceptionHandler) 작성
- [ ] User Entity 작성
...

💡 다음 작업: 공통 응답 클래스(ApiResponse) 작성
```

## 주의사항

- staged 파일이 없으면 작업하지 않음
- 커밋 메시지는 반드시 사용자 확인 후 진행
- 체크박스 업데이트 시 잘못된 항목을 체크하지 않도록 주의